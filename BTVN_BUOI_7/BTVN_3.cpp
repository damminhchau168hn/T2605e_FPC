#include <stdio.h>
#include <stdlib.h>
int main () {
	int n; 
	printf ("nhap so n =");
	scanf ("%d", &n);
	int arr[n];
	int x;
	int max;
	int dis;
	for (int i=0; i <n; i++){
		printf("nhap so %d", i+1);
		scanf ("%d", &arr[i]);
		int max=abs (arr[0]-x);
	max = abs(arr[0]-x);
	if (abs(arr[i])-x)> max) {
		max = abs (arr[i]-x);
		printf("So xa X nhat %d", arr[i]);
		}
	}


}
